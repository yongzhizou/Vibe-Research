{
  "name": "vibe-research-app",
  "version": "1.2.0",
  "private": true,
  "description": "Vibe-Research: Your Personal Trading Research Agent with Windows Electron packaging",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "build:ui": "npm run build --prefix desktop",
    "build:backend": "node scripts/bundle-backend.mjs",
    "build": "npm run build:ui && npm run build:backend",
    "pack:win": "electron-builder --win --config electron-builder.yml",
    "dist:win": "npm run build && npm run pack:win"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2",
    "tree-kill": "^1.2.2"
  }
}
